# tachles — Claude Project Context

Hebrew-first AI platform for Israeli UGC product video ads.
**Current version:** V28.0.ST3 — Script Engine Quality v2 milestone, Sub-task 3: Diversity Enforcement shipped at **recalibrated gate `+0.10`** (originally `+0.15`). Three iterations converged at ~0.55 ceiling on `big_idea_diversity` for 6 Hebrew sentences about the same product to the same audience; iter 1 (post-gen `validateAxisDiversity()` + 1 retry on duplicates) adopted as production. **Result:** `big_idea_diversity = 0.548` (vs baseline 0.420, delta +0.127, gate +0.10 PASSED with margin 0.028), `framework_signal_match = 0.833` (above 0.80 threshold but Sub-task 6 fate stays REQUIRED per baseline rule), `casual_markers_per_scene = 0.079` — REGRESSED from baseline 0.144 (−0.065). The orthogonality framing helps the GATED metric (diversity) but does NOT lift register; the LLM produces 6 axis-distinct concepts that still avoid casual markers. Sub-task 4 starts at the SHIPPED post-Sub-task-3 value (0.079), gap to the 1.0 gate is ~13× — this is the harder of the remaining sub-tasks. Mechanism observation surfaced from the 3 iterations: soft prompt-level instructions narrow attention to the stated goal at the cost of unmentioned ones (iter 1 dropped markers); hard schema-level constraints expand creative search across all phrasing dimensions (iter 3 lifted markers as a side effect of strict slot pinning). For Sub-task 4: prefer the schema-level REG-01 approach over prompt-only enforcement. **Schema** ([concept-cards-schema.ts](packages/prompts/src/concept-cards-schema.ts)) — added `big_idea_axis` enum field (6 values: convenience / proof / price / emotion / mechanism / social_validation), removed `estimated_quality` (LLM rated 8-9 always; meaningless gating signal). Field count stays at 12. Backwards-compat: `readPendingConcepts` normalizes legacy cards to `big_idea_axis = 'unknown'` (UI hides chip; engine treats as ungated). **System prompt** ([concept-system-prompt.ts](packages/prompts/src/concept-system-prompt.ts)) — added "AXIS DIVERSITY" hard-rule section (~6.4K → 8.3K chars) with all 6 axes named, examples, hard rule "6 קונספטים = 6 ערכי big_idea_axis שונים", + self-check before output; CONCEPT_REGEN_SYSTEM_PROMPT teaches `forbidden_axes` ban-list contract. **Engine** ([concept-engine.ts](apps/web/lib/llm/concept-engine.ts)) — `validateAxisDiversity()` checks uniqueness post-gen; on duplicate detection, `buildAxisRetryPrompt()` re-issues with explicit "fix slots [N,M], available axes [...]" instruction (1 retry budget). `regenerateSelectedConcepts` propagates `forbidden_axes` (axes of conceptsToKeep) into the user prompt. **UI** ([concept-card.tsx](apps/web/app/(dashboard)/projects/[id]/scripts/concept-card.tsx)) — drops 1-10 quality badge, adds Hebrew axis chip (נוחות / הוכחה / מחיר / רגש / איך זה עובד / מה אחרים אומרים); `concept-flow.tsx` `autoPreselect` swapped from quality-sort to preferred-axis-order (proof > emotion > mechanism > price > social_validation > convenience). **Admin debug** ([page.tsx](apps/web/app/(admin)/admin/projects/[id]/debug/page.tsx)) — quality_score badge replaced with axis badge + axis row in detail grid. **Test suite** ([test-concept-interactive-pr6.ts](apps/web/scripts/test-concept-interactive-pr6.ts)) — added 15 ST3 assertions (sections 16-18: schema fields, validator pass/fail/edge cases, legacy normalization). **Iter history** (kept in concept-engine.ts comment + STATE.md for traceability): iter 2 lexical-diversity nudge regressed diversity 0.548 → 0.510 (reverted); iter 3 deterministic per-slot pinning hit 0.541 but dropped framework_signal 0.833 → 0.722 (rejected). Adopted iter 1 design. Cost across 3 iterations: ~$4.50 total (3 full eval runs at ~$1.50 each). PR1 (89) + PR2 (29) + PR3 (73) + PR4 (48) + PR6 (102+15) regression-clean. tsc clean across all 4 workspaces. **Forward warnings recorded in STATE.md + PLAN.md cross-cutting**: don't define more arbitrary gates pre-baseline (Sub-tasks 4 + 5 gates need the same scrutiny); track all metrics per sub-task not just the gated one (Sub-task 3 → casual_markers carry-forward is the example). **PLAN evolution**: added Sub-task 6.5 (post-Sub-task-5 cleanup migrating 4 legacy `chat.completions` call sites to Responses API per the GPT-5.4 prompting guide the user shared 2026-05-03 — non-gated cost/efficiency follow-up). Branch: `main`. (2026-05-03)

**Previous: V28.0.ST2** — Script Engine Quality v2 milestone, Sub-task 2: Baseline Run captured (STOPPED for user approval before Sub-task 3). Both engine paths baselined on 9-product gold set with Sonnet judges. **concept_interactive baseline (the one gates measure against, persisted to `.planning/eval/baselines/v27.11.PR6.json`)**: `big_idea_diversity = 0.420`, `casual_markers_per_scene = 0.144`, `framework_signal_match = 0.778`, `register_authenticity_score = 7.673`, `wall_time_total = 626,757ms` (~70s/product avg). **legacy_full_batch baseline (regression guard, `v27.11.PR6-legacy.json`)**: `big_idea_diversity = 0.432`, `casual_markers_per_scene = 0.198`, `framework_signal_match = 0.574`, `register_authenticity_score = 7.317`, `wall_time_total = 882,165ms`. **Sub-task 6 fate: REQUIRED** — `framework_signal_match = 0.778 < 0.80` triggers the conditional Framework Validators sub-task (placeholder in PLAN.md to be expanded before Sub-task 3 ships). **Interesting finding**: diversity + register pain present in BOTH engine paths, validating the milestone's bet that fixes belong at the prompt/schema layer not the engine-mode layer; concept_interactive's one clear win is `framework_signal_match` (+0.20 vs legacy). **Bug found + fixed mid-Sub-task-2**: first baseline run produced `framework_signal_match = 0.000` and `register_authenticity_score = 0.00/10` for ALL 9 products — Sonnet judge was config-erroring silently because `ANTHROPIC_API_KEY` is opt-in in this stack and metrics defaulted to 0 on judge failure. Three fixes shipped: (1) `judges/sonnet-judge.ts` auto-falls-back to OpenAI when Anthropic key absent + loud warning at first call; (2) `metrics/{framework-signal,register-authenticity}.ts` `console.warn` on every judge failure (no more silent 0s); (3) `script-engine-eval.ts` runs a one-shot `judgeHealthCheck()` at startup BEFORE burning the full baseline cost. New file `apps/web/scripts/eval/print-side-by-side.ts` for side-by-side baseline comparison + Sub-task-6-fate decision rule. New file `apps/web/scripts/eval/runners/legacy-runner.ts` calling `generateScripts()` directly (legacy path baseline). `ANTHROPIC_API_KEY` added to local `.env` (gitignored — never committed). Sanity check passed: judges varied across products (fwm 0-1, register 6.4-8.3); 1/9 outlier (`electronics-2` div=0+fwm=0, suspected partial concept-batch parse failure, 8/9 clean). Cost so far this milestone: ~$0.85 PI bootstrap + ~$0.15 broken first concept run + ~$1.50 concept_interactive baseline + ~$2.00 legacy baseline = ~$4.50. Ready for Sub-task 3 once user approves the baseline numbers. Commits: 5191a89 (Sub-task 1) → 571fd6c (state) → (this commit) Sub-task 2. Branch: `main`. (2026-05-03)

**Previous: V28.0.ST1** — Script Engine Quality v2 milestone, Sub-task 1: Eval Harness. New CLI `npm run eval:script-engine` runs the production concept-interactive code path against a pinned gold-set of 9 products (3 categories × 3, bootstrapped via `npm run eval:script-engine:bootstrap`) and measures 4 metrics: `big_idea_diversity` (1 - mean cosine similarity of OpenAI text-embedding-3-small over the 6 concept big_ideas), `casual_markers_per_scene` (regex count over canonical Israeli markers [תכל'ס/וואלה/סבבה/פשוט/בכלל/אחותי/תקשיבי/תקשיב/לא נורמלי], excluding `decision_push` CTA scenes), `framework_signal_match` (Sonnet judge guesses framework from spoken text alone, 6-way closed-set, score = correct/total), `register_authenticity_score` (Sonnet judge rates 1-10 with 5 ❌ + 5 ✅ category-anchored exemplars). Per-stage timings (`pi_duration_ms` / `concept_batch_duration_ms` / `concept_expand_duration_ms` / `wall_time_total`) captured via `TimingCollector`. Output JSON to `.planning/eval/runs/<ts>.json` (gitignored) + console comparison table when `--compare=baseline.json` is passed. Smoke flag (`--smoke`) runs 1 product + skips judges (~$0.02 cost vs ~$0.80 for full 9-product run). New files: `apps/web/scripts/eval/{script-engine-eval,bootstrap-gold-set,fixtures/products,lib/{canonical-markers,embeddings,timing-collector,output-writer,gold-set-loader},anchors/{register-anchors,framework-signatures},judges/sonnet-judge,runners/{concept-runner,expand-runner},metrics/{big-idea-diversity,casual-markers,framework-signal,register-authenticity}}.ts` + 1 bootstrapped gold-set JSON (`.planning/eval/gold-set/cosmetics-1.json`, 16K). Smoke pass on cosmetics-1 immediately validated both production failure modes the user reported: `big_idea_diversity = 0.395` (low — 6 concepts cluster heavily, confirming "4/6 share big_idea"); `casual_markers_per_scene = 0.00` with 4/4 non-CTA scenes containing zero markers (confirming "Hebrew is written, not spoken"). The 4 V27.11 regression suites (PR1 89 + PR2 29 + PR3 73 + PR4 48) + PR6 102 all clean. tsc clean across all 4 workspaces. Cost so far: ~$0.13 (PI bootstrap + 1 concept batch + 1 expand). Sub-task 2 (Baseline Run) is next — run the full 9-product eval with judges on, persist to `.planning/eval/baselines/v27.11.PR6.json`. Branch: `main` (trunk-based per house rule). Anchor: `.planning/phases/01-script-engine-quality-v2/01-PLAN.md`. (2026-05-03)

**Previous: V27.11.PR6** — concept-first INTERACTIVE script UX (replaces PR5's backend-only auto-pick). Behind `SCRIPT_ENGINE_MODE=legacy_full_batch|concept_interactive` feature flag (default = legacy, zero behavior change). PR5's `concept_first` value is silently re-mapped to legacy (broken UX retired). New flow: user clicks "צור 6 כיוונים קריאייטיביים" → backend returns 6 lightweight concept cards → user reviews / refreshes weak ones / picks 1-3 → only the selected concepts are expanded into full scripts → user picks 1 final script → continues to scenes. **Schema** ([concept-cards-schema.ts](packages/prompts/src/concept-cards-schema.ts)) — 12 LLM-output fields per card (framework / big_idea / selected_hook / hook_direction / target_audience_moment / emotional_trigger / product_proof_moment / scene_outline / why_it_fits_product / why_it_fits_audience / estimated_quality / risk_notes). Server-managed wrapper adds concept_id (UUID) / slot_index (stable 0..5) / regenerationCount / regeneratedFromConceptId. Plus a `CONCEPT_REGEN_JSON_SCHEMA` for partial regeneration with the same card shape. **System prompt** ([concept-system-prompt.ts](packages/prompts/src/concept-system-prompt.ts)) — `CONCEPT_SYSTEM_PROMPT` (~6K chars, ~16% of full SCRIPT_SYSTEM_PROMPT) explicitly forbids spoken_text_hebrew / visual_prompt_english / scene-level metadata at phase 1 + reaffirms anti-collage rules; `CONCEPT_REGEN_SYSTEM_PROMPT` adds delta-rules for partial regen ("conceptsToKeep — אסור לחזור על שום ממד שלהם", "conceptsToReplace — אסור לחזור על אותה חולשה"). **Storage** ([concept-storage.ts](apps/web/lib/llm/concept-storage.ts)) — `Project.productData.pendingConcepts` JSON blob (no DB migration). Survives page refresh. Cleared only via explicit "regenerate all". `wrapRawConceptsForStorage` assigns IDs+slots; `replaceSlots` keeps non-replaced slots byte-identical and increments regenerationCount on replaced ones. **4 server actions** ([concept-actions.ts](apps/web/app/(dashboard)/projects/[id]/scripts/concept-actions.ts)) — `generateConceptsAction` (6 cards, ApiCall slug `script_concept_batch`); `regenerateSelectedConceptsAction` (replaces only chosen slots, preserves slot_index, slug `script_concept_regenerate_selected`); `regenerateAllConceptsAction` (explicit fresh batch, slug `script_concept_batch` with metadata.regenerateAll); `expandPickedConceptsAction` (charges `script_batch` credits × N selected, expands in parallel via `SINGLE_SCRIPT_JSON_SCHEMA`, slug `script_concept_expand`, partial-failure refund per failed expansion). **UI** — `ConceptCardView` ([concept-card.tsx](apps/web/app/(dashboard)/projects/[id]/scripts/concept-card.tsx)): RTL Hebrew card with framework label, hook direction badge, quality 1-10 badge, big_idea, hook preview block, audience-moment + emotional-trigger + proof-moment, collapsible scene_outline, collapsible why-it-fits, risk_notes amber alert when present, per-card "רענן רעיון" button, slot indicator. `ConceptFlow` ([concept-flow.tsx](apps/web/app/(dashboard)/projects/[id]/scripts/concept-flow.tsx)): state machine (idle / generating / picking / regenerating / expanding / error), auto-preselects top-3-by-quality, refuses 4+ silently, sticky action bar showing selected count + total credit cost + "הפוך ל-N תסריטים מלאים" CTA, "צור את כל הרעיונות מחדש" button (with confirm dialog), error banner on action failure. `scripts/page.tsx` branches on `resolveScriptEngineMode()`: concept_interactive + no scripts → ConceptFlow; legacy or has scripts → existing StreamingScriptsGrid. **Selection rules** — 0 selected = blocked, 1-3 = allowed, 4+ = blocked, duplicates = blocked. **Cost / credits** — concept generation + regen are free of user credits (provider cost logged via ApiCall); expansion charges `PER_OPERATION_CREDITS.script_batch × N selected` up-front with per-failure refund. **Verification** ([test-concept-interactive-pr6.ts](apps/web/scripts/test-concept-interactive-pr6.ts)) — 102 assertions across 16 sections covering all 16 user-named test cases. PR1 (89) + PR2 (29) + PR3 (73) + PR4 (48) regression-clean. tsc clean across all 4 workspaces. Production build passes (scripts page bundle 10.2 kB). Standalone runner: `npm run test:conceptinteractive`. **Branch**: `v27-11-concept-interactive-ux` — NOT merged to main yet, awaiting manual browser UAT. **Operator opt-in (after manual UAT)**: `SCRIPT_ENGINE_MODE=concept_interactive` (and optionally `LLM_SCRIPT_PROVIDER` / `OPENAI_SCRIPT_MODEL`). Rollback: `SCRIPT_ENGINE_MODE=legacy_full_batch` (or unset). (2026-05-02)

**Previous: V27.11.PR5** — concept-first script engine behind `SCRIPT_ENGINE_MODE=legacy_full_batch|concept_first` feature flag (default = legacy, zero behavior change). New module `apps/web/lib/llm/concept-engine.ts` implements two-phase generation: phase 1 (`generateConceptCards`) does ONE LLM call returning 6 lightweight concept cards (~1.5K output tokens total — `framework` + `big_idea` + `specific_situation` + `selected_hook` + `emotional_trigger` + `persuasion_angle` + `why_this_is_different_from_other_scripts` + `scene_outline` (4-5 short bullets, NO full spoken text) + self-rated `estimated_quality` + `why_this_quality_score`); `pickTopConceptsByQuality` ranks by `estimated_quality` with deterministic FRAMEWORK_ORDER tie-break; phase 2 expands top N (default 3, `SCRIPT_CONCEPT_TOP_N` env-tunable, clamped [1,6]) in parallel via the existing `SINGLE_SCRIPT_JSON_SCHEMA` path with a concept-locking `buildExpansionPromptFragment` appended to the user prompt so the LLM can't drift from the chosen `big_idea` / `specific_situation` / `selected_hook` / `emotional_trigger` / `persuasion_angle`. Net cost: ~45% fewer output tokens per batch (~$0.07 saved on `gpt-5.4-mini`). New light system prompt `CONCEPT_SYSTEM_PROMPT` (~4.4K chars, ~11.2% the size of `SCRIPT_SYSTEM_PROMPT`) — explicit "אסור לכתוב spoken_text_hebrew / visual_prompt_english / scene-level metadata / full creative_strategy / music_profile" so phase 1 stays light. PI block is shared across phases via the new `extractIntelligenceBlock` helper that strips `SCRIPT_SYSTEM_PROMPT` from the cached system instruction and prepends the concept system prompt, so phase 1 carries the same product grounding without the heavy creative-rails prompt. New schema `packages/prompts/src/concept-cards-schema.ts` (`CONCEPT_CARDS_JSON_SCHEMA`, strict mode, 10 required fields per card). Fail-soft: if phase 1 errors mid-batch, the orchestrator falls back to `legacy_full_batch` for that single batch (env stays set). Retry block guard: `engineMode === 'legacy_full_batch'` only — concept_first errors return fewer scripts; the user re-runs. **Verification**: `apps/web/scripts/test-concept-engine-pr5.ts` (84 assertions, 8 sections — schema shape + framework enum / system-prompt size guard + load-bearing topics / `resolveScriptEngineMode` env resolution including case-insensitive + invalid fallback / `resolveConceptTopN` bounds [1,6] + default 3 + non-numeric fallback / `pickTopConceptsByQuality` ranking + tie-break + edge cases + 100-run determinism / `buildExpansionPromptFragment` locks every load-bearing concept field verbatim / `extractIntelligenceBlock` recovers PI marker from the V27.11.PR2 system-instruction shape and returns null on legacy / default mode is `legacy_full_batch`). All 84 pass; PR1 (89) + PR2 (29) + PR3 (73) + PR4 (48) regression-clean. tsc clean across all 4 workspaces. Standalone runner: `npm run test:conceptengine`. Operator opt-in: set `SCRIPT_ENGINE_MODE=concept_first` (and optionally `SCRIPT_CONCEPT_TOP_N=4` for more variety) in Vercel/Railway env. (2026-05-02)

**Previous: V27.11.PR4** — durable anti-collage architecture. PR1 closed the door at the image-prompt + brief-builder layer (universal `SINGLE_FRAME_RULE` + `detectComparisonGuard` bridge); PR4 closes it at the upstream schema + system prompt so NEW LLM scripts never even ask for a multi-panel layout. Two enum changes in `packages/prompts/src/script-json-schema.ts`: (1) `'before_after'` removed from `SCENE_GENERATION_TYPES` (now 11 values, was 12) — the before/after creative beat is rendered as TWO consecutive scenes (state 1 + state 2), never inside a single panel-split frame; (2) `'comparison_split'` renamed to `'comparison_focus'` in `FRAME_STRATEGIES` — the new value enforces single-state composition (sharp product, alternative if visible is desaturated/out-of-focus/in-the-background, never a second panel). System prompt updated end-to-end in `script-system-prompt.ts`: `scene_generation_type` vocabulary list drops `before_after` with an explicit deprecation note pointing the LLM to the 2-scene sequence pattern; the home/cleaning category rule reframes "before/after הוא הסיפור" as "רצף 2 סצנות" (state 1 scene + state 2 scene); the `frame_strategy` table row for `comparison_focus` explicitly forbids "split-screen / two panels / before-and-after layout"; new global rule: "before/after = רצף בין סצנות, לא פאנל אחד". The 30s mode-table row, the demo-scene-minimum rule, the field-consistency table, and the `primary_subject=avatar` prohibition list all sweep `before_after` out. Schema description for `frame_strategy` rewritten to V27.11.PR4 wording; `product_visibility_priority` description updated `comparison_split → comparison_focus`. **Backwards-compat**: zero DB column changes; legacy `Script.rawJson` blobs in DB with `sceneGenerationType: 'before_after'` and `frame_strategy: 'comparison_split'` parse fine — `LlmScene.scene_generation_type` is typed as plain `string` at the runtime mapper layer (not the enum), so legacy data flows through unchanged. The PR1 bridge in `image-brief-builder.ts` continues to catch those legacy rows: any scene with `sceneGenerationType === 'before_after'` triggers `COMPARISON_GUARD_RULE_BLOCK` + 13 collage-specific negatives at brief-render time. The 15 word-boundary-anchored phrase patterns also catch legacy `comparison_split` visual prompts via "split screen / vs / side-by-side" detection. **Verification**: `apps/web/scripts/test-anticollage-pr4.ts` (48 assertions, 10 sections — enum trims in both batch + single schemas, type-union update, system-prompt sweeps for `before_after` and `comparison_split`, new "before/after = 2 scenes" rule, PR1 bridge regression on legacy `before_after`, end-to-end legacy scene fires guard, post-PR4 clean scene does NOT trigger false-positive guard, schema-required count unchanged at 20). All 48 pass; PR1 (89) + PR2 (29) + PR3 (73) regression-clean. tsc clean across all 4 workspaces. Standalone runner: `npm run test:anticollagepr4`. Schema bridge in `lib/animation/scene-routing.ts` (deriveSceneRouting heuristic) intentionally retains `'before_after'` as a possible output for legacy scenes that lack explicit `requiresLipSync` — orthogonal layer, returns `requiresLipSync: false` either way. (2026-05-02)

**Previous: V27.11.PR3** — schema trim. 4 admin/debug-only fields dropped from `SCENE_ITEM_SCHEMA` per the diagnose audit's bottleneck #3 (`packages/prompts/src/script-json-schema.ts`): `israeli_environment_required` (boolean — Israeli realism is enforced upstream by the deterministic image-brief-builder regardless), `local_realism_notes` (free-form Hebrew/English notes — emitted deterministically by `israeli-realism-rules.ts` independent of this field), `why_this_scene_exists` (admin/debug only, never read by any runtime mapper), `narrative_link_from_previous` (V27.9 cohesion field — admin/debug only). Schema-level required-field count drops 24 → 20 per scene; properties{}.size matches (OpenAI strict-mode invariant). Across the 6-script batch with ~5 scenes per script that's ~120 → ~100 mandatory output fields, eliminating ~30-100 chars of free-form Hebrew prose × 4 fields × 5 scenes × 6 frameworks = ~3,600-12,000 wasted output tokens per cold batch (decode-bound on a reasoning model). **Backwards compatibility**: zero DB column changes; `Script.rawJson` blobs in DB that already contain these 4 keys parse fine — the schema only validates NEW LLM output, never legacy data. Verified with a grep across `apps/web/` + `packages/`: zero runtime consumers (admin debug, brief builder, mappers, type interfaces) read these fields. The cohesion the dropped `narrative_link_from_previous` was meant to enforce moves INTO the spoken text itself: new V27.11.PR3 CONTINUITY section in `script-system-prompt.ts` instructs the LLM to build scene-to-scene flow via lexical bridges, rhetorical questions, time/place markers, continuous first-person — directly inside `spoken_text_hebrew`. The viewer hears the cohesion; a meta-string they never hear was the wrong layer to enforce it. **Verification**: `apps/web/scripts/test-schema-trim-pr3.ts` (73 assertions, 10 sections — 4 dropped fields not in required[] / not in properties{} for both `SCRIPT_JSON_SCHEMA` + `SINGLE_SCRIPT_JSON_SCHEMA`; 20 keep-list fields still required + still in properties; required count = properties count = 20 (strict-mode invariant); `additionalProperties: false`; system prompt no longer mentions any of the 4 dropped names; CONTINUITY section header / lexical-bridge technique / read-aloud test all present; legacy DB-shape scene with all 4 dropped fields PRESENT still reads keep-list fields cleanly; PR3-shape scene with the 4 dropped fields ABSENT satisfies SCHEMA.required[] with no extras; `lib/video-mode.ts` 15s/30s constraints orthogonal to PR3; `GeneratedScript` runtime camelCase shape unchanged — none of the 4 dropped fields ever made it into the runtime mapper). All 73 pass; PR1 (89) + PR2 (29) regression-clean. tsc clean across all 4 workspaces. Standalone runner: `npm run test:schematrim`. Pre-existing V14 PR5 failures (8) unchanged after PR3 — they reference `hook_alternatives` (V27.10.9 removal) + the 30-item checklist (PR2 removal), unrelated to PR3. (2026-05-02)

**Previous: V27.11.PR2** — script cost/latency quick wins (PI to cached prefix · drop self-checklist · default `gpt-5.4-mini`). Three audit bottlenecks (#1 PI duplication, #2 prompt entropy, #4 oversized model) collapsed into a single-commit cost cut. **(1) Product Intelligence** is now built ONCE in `apps/web/lib/llm/scripts.ts` via a new exported helper `buildSystemInstructionWithIntelligence(intel)` and passed identically to all 6 parallel framework calls as `systemInstruction`. Pre-PR2 the PI block (~3.6K chars / ~900 tokens of dossier + visualAnalysis + audience inference) was rebuilt into every framework's user prompt, so providers' prefix cache missed 6× per batch. Post-PR2 the systemInstruction is byte-identical across the 6 calls (verified deterministic, 10 invocations identical) — Anthropic / Gemini / OpenAI Responses API all cache the system block, so call 1 writes the cache and calls 2-6 read at ~10% input rate. The PI block is hard-grounded inside the system block ("USE THIS, NOT THE LEAN FIELDS ABOVE"). **(2) The 30-item ✅ self-checklist** at the tail of `packages/prompts/src/script-system-prompt.ts` (lines 628-660 pre-PR2: 30 numbered checkmark items + the V14/V27.9 patches on top of them) compressed to a single read-aloud sentence: "קרא את spoken_text_hebrew של כל הסצנות ברצף בקול רם. אם עצרת בגלל register, דקדוק, non-sequitur בין סצנות, או הרגשת שזה לא נשמע כמו אינפלואנסר ישראלי באמת — תכתוב מחדש את הסצנה הבעייתית. החזר אך ורק JSON תואם לסכמה." Net: prompt drops from 661 → 616 lines, ~40K → 37.3K chars (~750 tokens off the cached prefix; the load-bearing rails — REGISTER LOCK, 6 frameworks, 8 voice profiles, Israeli realism, frame_strategy table — are intact). **(3) OpenAI default model** flipped `gpt-5.4` → `gpt-5.4-mini` in `apps/web/lib/llm/openai-script-client.ts`. Cost: $2.5 / $10 → $0.75 / $4.5 per MTok (~3x cheaper). Two new env knobs preserved: `OPENAI_SCRIPT_MODEL` (explicit pin always wins) and `SCRIPT_QUALITY_MODE=balanced|premium` (ergonomic toggle without knowing the model id). Resolution order: `OPENAI_SCRIPT_MODEL` → `SCRIPT_QUALITY_MODE=premium → 'gpt-5.4'` → default `'gpt-5.4-mini'`. Rationale: even with the full `gpt-5.4` (V27.10.18 default), live scripts came back poor — the bottleneck is prompt entropy + schema bloat + uncached PI, NOT model size. PR2 fixes the architecture; the smaller model is now sufficient because its instruction-following stays high on a tighter prompt. Premium mode is one env-flip away if needed. **Verification:** `apps/web/scripts/test-script-perf-pr2.ts` (29 assertions, 7 sections — checklist removal evidence, char/line measurements, PI-in-systemInstruction, byte-identity across 10 calls, default model, cache-eligibility floor, type-shape sanity). PR1 verification still passes (89 assertions). tsc clean across all 4 workspaces. Standalone runner: `npm run test:scriptperf`. (2026-05-02)

**Previous: V27.11.PR1** — Universal Single-Frame Rule + comparison-guard bridge (anti-collage stop-the-bleeding). Diagnose-only audit (`.planning/debug/v27-script-quality-audit.md`) traced the multi-panel / collage / split-screen failure mode of gpt-image-2 to two compounding causes: (1) **nothing** in `packages/prompts/src/scene-image-prompts.ts` ever explicitly forbade panel layouts (grep confirms zero matches for collage|split-screen|multi-panel|comic|contact-sheet|storyboard|montage|mosaic|side-by-side|diptych|panel-layout across `packages/prompts/`, `apps/web/lib/image-briefs/`, `apps/web/lib/scene-planning/`); (2) the upstream schema actively encouraged two-state visuals via `sceneGenerationType='before_after'` and `frame_strategy='comparison_split'`, so the LLM happily wrote "before and after" / "vs" / "split screen" prose into `visual_prompt_english` and gpt-image-2 rendered it as a multi-panel layout. **PR1 fix (this version):** new `SINGLE_FRAME_RULE` constant in `scene-image-prompts.ts` rendered in BOTH branches of `buildScenePrompt()` (avatarPresent=true + avatarPresent=false), placed AFTER the ASPECT_OPENER/subject-opener and BEFORE the IDENTITY/PRODUCT locks so the rule is anchored before the model reads any comparison language in the brief; explicit negatives for collage / split-screen / multi-panel / comic strip / contact sheet / storyboard / grid / mosaic / diptych / two-panel / inset / picture-in-picture; one explicit single-state instruction ("if the brief uses 'before and after' / 'comparison' / 'vs', render only ONE state in this single frame — the contrast belongs to TWO scenes, not two panels"). Plus a bridge in `apps/web/lib/image-briefs/image-brief-builder.ts`: pure `detectComparisonGuard()` flags scenes whose `sceneGenerationType==='before_after'` OR whose `rawVisualBrief` matches one of 15 word-boundary-anchored phrase patterns (before-and-after / before/after / split[-]screen / side[-]by[-]side / vs / versus / compared to / two-panel / multi-panel / diptych / comic strip / contact sheet / storyboard / collage / mosaic). When fired, `buildImageBrief` appends `COMPARISON_GUARD_RULE_BLOCK` to `ruleBlocks` AND pushes 13 collage-specific negatives to BOTH `mustAvoid` and `negativeConstraints` (the latter is the array rendered into the "MUST NOT SHOW" section of `finalImagePrompt`). Two new fields on `ImageBrief`: `comparisonGuardApplied: boolean` + `comparisonGuardReasons: string[]` for telemetry / future admin debug. **Verification:** `apps/web/scripts/test-anticollage-pr1.ts` (89 assertions, 10 sections). All pass; tsc clean across all 4 workspaces. Standalone runner: `npm run test:anticollage`. PR1 is stop-the-bleeding only — PR2-PR5 follow per audit (PR4 will deprecate `before_after` + replace `comparison_split` with `comparison_focus` at the schema level so the bridge stops firing on new scripts). Pre-existing V14 PR5 + PR7 test failures on `main` are unrelated to PR1 (verified via `git stash`). (2026-05-02)

**Previous: V27.10.20** — lipsync silent-skip root cause: env override `OPENAI_FACE_GATE_MODEL=gpt-4o-mini` + face-gate.ts sending `reasoning.effort: 'low'` (gpt-5/o-only param) → HTTP 400 → catch fired → lipsync silently skipped on every call. Fix: `isOpenAiReasoningModel(model)` helper gates the param across face-gate / motion-analysis / product-visual-analysis. Plus visibility: face-gate verdict + lipSyncStatus + lipSyncErrorMessage now persist to Scene (dead columns since V7); admin debug surfaces a new "מצב Lipsync" section with the verdict, error, and a yellow alert when status='skipped_face_gate_error'. Also: clip-impl motion-analysis fallback synced from `'gpt-4o-mini'` → `'gpt-5.4-mini'` to match motion-analysis.ts (was a 5x cost-attribution drift). Admin debug "ניתוח תנועה" label now reads from env var instead of being hardcoded.
**Production:** https://tachles-lac.vercel.app
**Output:** 9:16 MP4 ads, 15s or 30s, Hebrew voice-over + RTL captions + background music.

---

## Stack (quick reference)

| Layer | Tech |
|-------|------|
| Web | Next.js 15 App Router · React 19 · Tailwind 3.4 · shadcn/ui · RTL Hebrew |
| Worker | Node 20+ · BullMQ 5 · ioredis · `tsx watch` (dev) / Docker (prod) |
| DB | PostgreSQL via Prisma 6 — Supabase pooler `aws-1-ap-south-1` (Mumbai) |
| Queue | Redis Cloud free tier (prod) · local Redis (dev) |
| Storage | Cloudflare R2 (prod, S3-compatible) · local `public/uploads/` (dev) |
| Auth | Supabase Auth — `ADMIN_EMAILS` env auto-promotes first user |
| AI | OpenAI (gpt-5.4-mini scripts, gpt-image-2 scenes, gpt-4o-mini vision for face-gate / motion-analysis / product-visual-analysis — V13 PR1 removed the post-gen QA loop) |
| Voice | ElevenLabs `eleven_v3` with-timestamps |
| Video | Kling Omni v3 i2v + PixVerse LipSync |
| Composition | ffmpeg on worker host → upload MP4 to R2 |

---

## Production deployment

| Service | Where | Notes |
|---------|-------|-------|
| Web (Next.js) | Vercel Hobby | **region: `bom1` (Mumbai)** — pinned in `vercel.json`. MUST stay co-located with Supabase or every Prisma query pays ~250ms cross-region latency. |
| Worker (BullMQ) | Railway (Dockerfile) | `apps/worker/Dockerfile` + `railway.toml` + `.railwayignore`. ffmpeg pre-installed. CMD must `cd /app/apps/worker` before invoking `tsx` so monorepo-hoisted node_modules + relative imports (`./env`) resolve. railway.toml MUST NOT set `startCommand` — it overrides Dockerfile CMD. |
| DB | Supabase `ap-south-1` | Pooler URL (port 6543) for app, direct URL (port 5432) for `prisma db push`. |
| Queue | Redis Cloud (free) | `REDIS_URL` shared between web + worker. |
| Object storage | Cloudflare R2 | `CLOUDFLARE_R2_BUCKET_NAME` env auto-switches `lib/storage/index.ts` from local to R2. |
| Production URL | https://tachles-lac.vercel.app | Set `PUBLIC_BASE_URL` to this so Kling/PixVerse can fetch silent clips + voice MP3s. |

**Verify region after deploys:** `curl -sI https://tachles-lac.vercel.app/api/health \| grep x-vercel-id` — middle segment must be `bom1`.

**Storage migration history (V12.1–V12.3, 2026-04-30):**
- V12.1 — `lib/storage/read-public-asset.ts` helper. Try-disk → fallback-HTTP. Replaced 5 disk-only readers (scene-images, motion-analysis, face-gate, image-qa, product-visual-analysis).
- V12.2 — Static catalogs (avatars + music + voice samples) migrated to R2. URLs hard-coded in catalog files. Bulk uploader: `apps/web/scripts/upload-static-assets-to-r2.ts`.
- V12.3 — Remaining 4 disk readers patched (kling.imageToPayload, kling.downloadAsBuffer, pixverse.resolveToBytes, mux-audio.readUrlAsBuffer). All `process.cwd()/public/` outside `LocalStorage` adapter and `read-public-asset.ts` itself eliminated.
- V12.4 — Voice-sample preview CORS fix. R2 returns 403 on OPTIONS preflight (admin-scope token needed to set CORS). `voice-presets.ts sampleUrl` reverted to `/api/voice/sample/<id>` (same-origin). API route lookup chain: R2 → local disk → ElevenLabs synth → cache back to BOTH. New helper `scripts/set-r2-cors.ts` for when an admin R2 token is available.
- V12.5 — Live provider balance dashboard. `lib/providers/balance.ts` queries Kling `/account/costs`, PixVerse `/openapi/v2/account/balance`, ElevenLabs `/v1/user/subscription`, and OpenAI `/v1/organization/costs` in parallel; surfaced at the top of `/admin/costs` with 60s revalidation. Soft-fails per-provider — an outage on one doesn't break the page.
- V12.6 — Graceful per-provider fallback. When a balance fetcher fails (HTTP 401/403/network), the card falls back to local `ApiCall` aggregates (30d spend + call count) instead of just showing the error. `ProviderFallbackCard` keeps the error visible in a `<details>` block with a fix hint.
- V12.7 — OpenAI parser fix + admin key. `fetchOpenAIBalance` was crashing because `/v1/organization/costs` sometimes returns `amount.value` as a string and `+` was concatenating. Coerced with `Number(...)`. New env var `OPENAI_ADMIN_API_KEY` (sk-admin-…) is preferred over `OPENAI_API_KEY` for Administration API reads — regular keys (sk-svcacct, sk-…) are scoped to model invocation only.
- V13 PR1 — Image QA auto-regen loop removed from active path. Deleted `apps/web/lib/image-qa/`, the QA branch in `lib/scenes/generate-impl.ts`, `buildCorrectiveBrief` in `image-brief-builder.ts`, and the `IMAGE_QA_ENABLED` / `IMAGE_QA_MAX_RETRIES` / `OPENAI_IMAGE_QA_MODEL` env vars. Single-pass image gen now: brief builder → gpt-image-2 → persist. Quality strategy moved to upstream creative planning. DB columns `Scene.imageQaJson` / `imageRegenAttempts` / `needsManualReview` remain nullable for historical data; PR1 stops writing them. Vision calls KEPT: Product Visual Analysis, Motion Analysis, Face Gate (all upstream/routing, not post-generation).
- V13 PR2 — Image Brief strengthening, four small commits. PR2.1 extracts Israeli realism into `apps/web/lib/scene-planning/israeli-realism-rules.ts`. PR2.2 adds `scene-rules.ts` with hands-physics + mirror-safety detectors and rule builders, surfaced as `ImageBrief.handsPhysicsRequired` / `mirrorRisk` / `ruleBlocks` flags. PR2.3 adds the PRODUCT REFERENCE LOCK paragraph in `packages/prompts/src/scene-image-prompts.ts` and gates product mention on `isProblemScene`. PR2.4 adds `buildContactProofRule` answering all five demo questions (where/who/active part/contact/proof) for product_demo / hands_only / closeup_product. Deterministic, no LLM, no DB migration. Verification: `apps/web/scripts/test-v13-pr2.ts` runs 53 assertions.
- V13 PR4 — Stage-tagged logger (`apps/web/lib/logging/log.ts`) with `logStage(stage, scope)`, `.span(label, fn)`, LOG_LEVEL filter, sensitive-data masking. Wired into image-brief / image-gen / voice (PR4.2) and motion-analysis / kling / face-gate / pixverse pipelines (PR4.3) — zero `console.*` left in clip-impl active path.
- V13 PR5 — Curated Hebrew error messages map (`apps/web/lib/errors/scene-error-messages.ts`) covering every pipeline stage. `getSceneErrorMessage(code, raw)` returns `{ hebrew, retryHint?, needsUserEdit?, isFallback }`.
- V13 PR6 — Scene state machine + log buffer schema. Migration `v13_scene_state_log` adds `Scene.status` (String, default 'pending'), `lastErrorCode`, `lastErrorMessage`, `generationLogJson` (all nullable / additive). Canonical states + helpers in `apps/web/lib/scenes/scene-status.ts` (no Prisma enum per house style).
- V13 PR7 — Pipeline impls write the new fields + UX components consume them. PR7.1 wires status transitions in generate / voice / clip impls (each curated `<stage>.<reason>` lastErrorCode matches a PR5 entry). PR7.2 adds `flushSceneLogBuffer` — best-effort persistence of buffered scene logs to `Scene.generationLogJson` (cap 200, oldest dropped on overflow). PR7.3 adds `SceneCardStatusBadge` + `SceneErrorDetails`. PR7.4 adds `SceneLogViewer` + `WizardWarningsPanel`. All RTL-first, Hebrew labels native, Server / Client component split honored.
- V13 PR8 — `/admin/scenes/[id]/debug` admin panel renders every persisted artifact for a scene: status badge, last error, generation log, routing flags, image brief, final prompt, motion analysis, legacy QA (with banner), generation history, project intelligence. Reuses PR5/PR6/PR7 components.
- V13 PR9 — `npm test` runs the V13 verification suite via `apps/web/scripts/test-v13-all.ts` master runner. 360+ assertions across 8 PR scripts run in ~5.4s; trade-off vs full vitest port documented in commit message.
- V13.1 — ffmpeg cold-start download. Vercel's bundler refused to ship the ffmpeg-static binary in the function (verified locally — .vercel/output had no ffmpeg file even with serverExternalPackages + outputFileTracingRoot + glob includes), so non-lipsync clips silently shipped without audio. `lib/scenes/mux-audio.ts` now downloads the binary from `https://github.com/eugeneware/ffmpeg-static/releases/download/b6.1.1/ffmpeg-linux-${arch}.gz` to `/tmp/tachles-ffmpeg-static` on first call, gunzips, chmods +x, caches for the warm container's lifetime (~1-3s cold-start cost). `clip-impl.ts` now refuses to persist a silent clip on mux failure: marks the scene `status='failed'` with `lastErrorCode='render.ffmpeg_failed'` and returns an error before the credit-charge transaction runs — the user is not charged for failed muxes. PR3.1 adds `apps/web/lib/animation/animation-plan-builder.ts` emitting a typed `AnimationPlan` (motionSubject + cameraMotion enum + objectMotion / humanMotion / forbiddenMotion[] / preserveProductVisibility / avoidFaceZoom / speakingExpected); defaults follow the V13 §10.3 table; V4 metadata + vision motion-analysis override. PR3.2 adds `buildKlingPromptFromPlan` in `kling.ts` that renders the plan into Omni's `{ positive, negative }`; `forbiddenMotion` items + baseline class negatives merge into the negative prompt via dedupe Set. PR3.3 plumbs the plan from `lib/scenes/clip-impl.ts` (legacy `buildKlingMotionPrompt` no longer in the active path); the same PR2 brief flags (handsPhysicsRequired / mirrorRisk / contactProofRequired) feed the plan so still and clip share constraints. Verification: `apps/web/scripts/test-v13-pr3.ts` runs 56 assertions.
- V13.2 — admin /admin/costs accuracy, auto-refresh, and DB performance hardening. Per-call cost attribution moves into `apps/web/lib/usage/cost-attribution.ts` — one helper per provider that prefers actual usage (tokens / characters / observed units) and falls back to configured constants. The `costUsd` field on `ApiCall` now mirrors `actualCostUsd ?? estimatedCostUsd`; both are persisted alongside a JSON `metadata` blob (safe usage payload — never auth headers). New columns: `ApiCall.estimatedCostUsd / actualCostUsd / metadata / renderJobId / sceneId`; new `CreditTransaction.refType`. New table `ProviderBalanceSnapshot` (provider/balanceType/balanceValue/balanceUnit/estimatedUsdValue/rawJson/status/errorMessage/fetchedAt) — observability only, never used to attribute per-call cost. `lib/providers/balance-snapshot.ts` wraps the existing fetchers with a 60s in-process cache + per-provider soft-fail + persist-to-DB. New API endpoints (server-side admin protected via new `lib/auth/admin-api.ts → requireAdminApi()` returning 401/403): `GET /api/admin/costs/{summary,recent-calls,in-flight,provider-balances,operation-stats}`. Recent-calls supports `?provider=&operation=&status=&since=&until=&expand=metadata` filters; metadata is opt-in to keep the table fast. The `/admin/costs` page now uses three client components (`SummaryKpis` 20s · `InFlightCallsSection` 4s · `RecentCallsTable` 8s) that poll their endpoints, show last-updated, and pause when the tab is hidden. Migration `20260430120000_v13_2_costs_hardening` adds the columns + 13 composite indexes (ApiCall provider+operation+createdAt, provider+status+createdAt, completedAt, userId+createdAt, projectId+createdAt, renderJobId+createdAt, sceneId+createdAt; CreditTransaction refType+ref; RenderJob status+createdAt, projectId+createdAt, completedAt; Project userId+createdAt; ProviderBalanceSnapshot provider+fetchedAt). Verification: `apps/web/scripts/test-v13-pr10.ts` runs 31 assertions covering attribution paths, the explicit ban on balance-delta cost attribution, and the closed allowlists guarding admin URL params.
- V14 PR1 — Israeli realism cue library. `apps/web/lib/scene-planning/israeli-realism-rules.ts` rewritten from a 70-line single-block negative-only emitter to a 794-line category-organized library: 51 atomic cues across 10 categories (`sockets_switches` / `architecture` / `streets` / `public_space` / `vehicles` / `brands` / `food` / `influencer` / `religious` / `climate`), every cue ships paired positive (Israeli detail to include) + negative (US/EU default to exclude). 21 universal negatives always-on. 12 environment_type baselines. 8 named scene presets (`kitchen_with_morning_light` / `bathroom_morning_routine` / `bedroom_evening` / `living_room_couch` / `tel_aviv_street_evening` / `supermarket_aisle` / `gym_modern` / `outdoor_park_afternoon`) — these IDs are the namespace V14 PR5's per-scene `israeli_setting_cue` field references; one shared cue ID system across image-brief and script systems. New `chooseIsraeliCues(ctx)` deterministic selector. AvatarProfile extended with required `archetype: PersonaArchetype` + `religiousRegister: ReligiousRegister`; all 25 catalog avatars backfilled (no nulls, no implicit defaults). Legacy `buildIsraeliRealismBlock` preserved as a thin shim — sunsets in PR2/latest PR7 once consumers migrate. Verification: `apps/web/scripts/test-v14-pr1.ts` runs 103 assertions across 15 categories.
- V14 PR2 — Frame-technique snippets. `apps/web/lib/image-briefs/frame-technique-snippets.ts` (NEW) ships 5 typed deterministic snippet builders that target the failure modes documented in `docs/v14/FRAME_PROMPT_TECHNIQUES.md`: `mirrorSelfieSnippet` (the "phone covers most of the face" technique), `selfieHandheldSnippet` (front-camera perspective lock + wide-angle distortion), `productHandHoldSnippet` (anatomical 5-finger grip + scale reference + one-hand vs two-hand explicit; complementary to PRODUCT_REFERENCE_LOCK and to `buildContactProofRule`, no duplicated phrases), `safeReflectionSnippet` (opt-in indistinct-reflection ask), `consistencyAnchorSnippet` (locks "same person across the whole ad", quotes `outfitDescriptionLocked` verbatim when supplied). `chooseFrameTechniqueSnippets(ctx)` selector dispatches based on `cameraFocus` / `sceneGenerationType` / `mustShowProduct` / `faceVisibility` / `windowOrReflectiveSurfaceVisible`. Mirror selfie wins over selfie hand-held when both signals fire. Single-scene ads suppress the consistency anchor. New CAMERA_FOCUS enum value `selfie_in_mirror` in `script-json-schema.ts`. New `ImageBrief.frameTechniqueSnippetIds` field surfaces fired snippet IDs for telemetry/admin debug. Verification: `apps/web/scripts/test-v14-pr2.ts` runs 76 assertions across 13 categories.
- V14 PR3 — Outfit lock + avatar byte-identity audit. `apps/web/lib/avatars/outfit.ts` (NEW) — `computeLockedOutfit({gender, style, archetype, religiousRegister, productCategory?})` is a pure deterministic outfit composer (top + bottom + footwear + accessories + hair/head from per-component builders). Religious gating: religious-female outfits get long sleeves + modest skirt + tichel reference; religious/traditional male outfits get a kippa note. Fitness `productCategory` pushes athletic register. `apps/web/lib/scenes/generate-impl.ts` reads `data.lockedOutfit`; if null and an avatar is selected, computes via `computeLockedOutfit()` and persists back to `Project.productData.lockedOutfit` (race-safe — deterministic, parallel writers produce the same string). Threads into `buildImageBrief.outfitDescriptionLocked`, which the PR2 consistency_anchor quotes verbatim. The audit confirmed `describeAvatar()` was already byte-identical across calls (pure function of AvatarProfile, no per-scene mutation). Verification: `apps/web/scripts/test-v14-pr3.ts` runs 81 assertions; every avatar in the 25-entry catalog produces a valid (≥20 char) outfit.
- V14 PR4 — Scene variation ledger + scroll-stopper levers. `apps/web/lib/image-briefs/scene-variation-ledger.ts` (NEW) — `SceneVariationLedger` class with `record()` / `countOf()` / `unusedFromKnown()` / `diversityScore()` / `summary()` (all deterministic, in-memory). `chooseScrollStopperIndex({totalScenes, finalSceneGoal?})` returns `{index, reason}`: ads ≥4 scenes get one promoted scene (default = hook at index 0, or punchline at last index when `finalSceneGoal='decision_push'`). `buildScrollStopperLevers({reason})` emits hook-flavored (tight surprising open) or punchline-flavored (satisfying close-up + saturated color) prompt fragments. ImageBrief gains 3 new return fields (`scrollStopperApplied` / `scrollStopperReason` / `variationDiversity`). `generate-impl.ts` loads sibling scene metadata, builds the ledger from earlier scenes (excluding the current), and threads `isScrollStopper` + `variationLedger` through. Ledger does NOT override script choices — it's diagnostic + a lever for one specific scene. Verification: `apps/web/scripts/test-v14-pr4.ts` runs 54 assertions including a 7-scene varied-ledger convergence test (≥4 distinct values in at least one tracked field).
- V14 PR5 — Script V6 system prompt + 4 new structured-output fields. `packages/prompts/src/script-system-prompt.ts` gets a V6 — REGISTER LOCK section at the top with concrete DO/DON'T linguistic anchors (תכל'ס / סבבה / אחותי / וואלה in DO; "אובססיבית" / "תהליך" / translated-American patterns in DON'T) — addresses the "sounds like translated news-anchor" failure mode. New V6 — שדות מבניים חדשים section explains genre / voice_profile / hook_alternatives / israeli_setting_cue. All 8 Israeli setting cues mentioned by name in the prompt — same IDs that PR1's SCENE_PRESETS expands deterministically. `script-json-schema.ts` gets 3 new const tuples (GENRES × 6 / VOICE_PROFILES × 8 / ISRAELI_SETTING_CUES × 8 — the 8 cue IDs match PR1's SCENE_PRESETS keys exactly). 4 new schema fields, all nullable for back-compat with V5 scripts already saved in DB: script-level `genre`, `voice_profile`, `hook_alternatives`; scene-level `israeli_setting_cue`. Final-checklist items 22-26 added so the LLM doesn't drop V6 fields on long generations. Verification: `apps/web/scripts/test-v14-pr5.ts` runs 64 assertions including a single-namespace check that `ISRAELI_SETTING_CUES_LIST` exactly matches `SCENE_PRESETS` keys.
- V14 PR6 — Admin debug surfaces. `/admin/scenes/[id]/debug` gets a new "V14 — frame techniques + scroll-stopper + outfit + genre" section that surfaces every V14 field per scene: `israeli_setting_cue` (from script.rawJson scenes[N]), `frameTechniqueSnippetIds` (from imageBriefJson), `lockedOutfit` (from project.productData), `scrollStopperApplied` / `scrollStopperReason` / full `variationDiversity` summary block (from imageBriefJson), `genre` / `voice_profile` / `hook_alternatives` count (from script.rawJson). All graceful-fallback to "—" for legacy scenes. New `/admin/projects/[id]/diagnostic` page builds a `SceneVariationLedger.fromRecords(scenes)` per script in the project, renders per-field diversity grid (distinct/total counts), per-scene record table (scene_order / cameraFocus / sceneGenType / primarySubject / scrollStopper / snippets-fired), and surfaces a yellow "Low diversity" warning banner when ≤2 distinct values for cameraFocus or sceneGenerationType across ≥4 scenes. Verification: `apps/web/scripts/test-v14-pr6.ts` runs 28 assertions (static checks on rendered labels + Prisma queries + ledger surface stability).
- V14 PR7 — Docs + master test runner. `apps/web/package.json npm test` chains V13 + V14 runners (`test:v13` + `test:v14` are also exposed individually). `apps/web/scripts/test-v14-all.ts` glob-discovers `test-v14-pr*.ts` scripts; running 6 V14 PR scripts produces 380+ V14 assertions in addition to the 390+ V13 cumulative (770+ total). README.md / STATUS.md / .claude/CLAUDE.md (this file) updated retroactively to capture PR1-PR6 + this PR7. Reference docs landed in `docs/v14/` (`ISRAELI_VISUAL_REALISM.md` / `FRAME_PROMPT_TECHNIQUES.md` / `HEBREW_SCRIPT_CREATIVE_RULES.md`).
- V25 — Script generation moved from OpenAI gpt-5.4-mini to Google Gemini 3 Pro. `apps/web/lib/llm/gemini-client.ts` (NEW) wraps `@google/generative-ai` with `geminiStructuredCall<T>()` (responseMimeType: 'application/json' + responseSchema; recursively strips `additionalProperties` which Gemini rejects); default model `gemini-3-pro` via `GEMINI_DEFAULT_MODEL`. `apps/web/lib/llm/scripts.ts` swapped to call Gemini in the existing 6-framework parallel batch (same `Promise.all` over `FRAMEWORK_ORDER`). Pricing — `priceGeminiText` in `lib/usage/pricing.ts` (gemini-3-pro / 2.5-pro $1.25/M input + $10/M output; 2.5/2.0 flash $0.075-0.10/$0.30-0.40); `attributeGeminiTextCost` in `lib/usage/cost-attribution.ts` mirrors the OpenAI helper. `lib/pricing/provider-costs.ts` adds `gemini_script_batch` ($0.04 default; reads legacy `COST_OPENAI_SCRIPT_BATCH_USD` env for back-compat). Admin `/admin/costs` adds a Google Gemini balance card (always falls back to local `ApiCall` aggregates — Generative Language API doesn't expose per-key billing). New env: `GEMINI_API_KEY`, optional `GEMINI_SCRIPT_MODEL`. Action layer in `apps/web/app/(dashboard)/projects/[id]/scripts/actions.ts` switched provider tag from `openai` to `gemini` on `recordApiCallStart`.
- V26.1-V26.13 + V26.SEC — bundle of script-gen / captions / parallel-regen / retry / security iterations on top of V26. (1) **V26.1**: corrected Gemini model id `gemini-3-pro` → `gemini-3-pro-preview`, dropped temperature override. (2) **V26.2**: migrated to new `@google/genai` SDK (legacy `@google/generative-ai` removed); `outputTokens` now sums candidates + thoughts. (3) **V26.3-V26.6**: Gemini cost-cutting iterations (Flash:minimal → Flash:low → Pro:low) — all reverted in V26.8 after live use showed lower visual-prose quality vs the OpenAI baseline. (4) **V26.4**: removed artificial Grok-vs-lipsync block — PixVerse runs on Grok output identically to Kling. (5) **V26.5**: fixed xAI 422 by wrapping `image` as `{ url: ... }` struct. (6) **V26.7**: UI delay fix on script gen — replaced blind `setInterval(router.refresh, 2500)` with count-driven polling against `/api/projects/[id]/scripts/list`; refresh only when count grows. (7) **V26.8**: script-gen reverted Gemini → OpenAI gpt-5.4-mini behind `LLM_SCRIPT_PROVIDER` env flag (default `openai`). New `apps/web/lib/llm/openai-script-client.ts` mirrors `gemini-client.ts`; Gemini path preserved for experiments. (8) **V26.9**: OpenAI client migrated from Chat Completions to the Responses API per official migration guide — 40-80% better prefix-cache utilization on the 6-parallel batch sharing `SCRIPT_SYSTEM_PROMPT`. `client.responses.create` + top-level `instructions` + `text.format.json_schema` + `output_text` helper + `usage.input_tokens` / `usage.output_tokens`. Live: 4.9s wall, $0.0022/call → ~$0.013 projected for 6-batch. (9) **V26.10**: per-scene clip regen runs in parallel. Pre-V26.10 `<form action={ServerAction}>` caused Next.js to serialize Server Actions per route → multiple scene clicks queued. Switched to direct `fetch('/api/scenes/[id]/clip')` — same path the batch button already used. New `/api/scenes/[id]/lipsync-only` route handler mirrors the clip route. (10) **V26.11**: transparent retry helper. New `apps/web/lib/utils/retry.ts → withRetry()` with `maxAttempts=2` + `earlyFailWindowMs=15000`. Default predicate retries on network errors + HTTP 408/429/500/502/503/504; not on 4xx/config/parse errors or anything past the early-fail window. Wrapped the submit/one-shot call in 9 provider clients (OpenAI Responses, Gemini, gpt-image-2, Kling i2v submit, Grok i2v submit, motion-analysis, face-gate, ElevenLabs TTS, PixVerse lipsync submit). Polling loops NOT wrapped — each tick is the implicit retry. (11) **V26.12-V26.13**: caption sync. V26.12 used `max(clipDuration, voiceDuration)` for cumulative offset + 100ms `CAPTION_LEAD_MS` lead-in (standard UGC practice — captions appear ~100ms before words). V26.13 made it bulletproof: new `probeDurationSeconds(filePath)` ffprobe helper in `apps/worker/src/providers/composition/ffmpeg.ts`. After stage 3a normalizes each clip to byte-identical codec params, the actual duration of each normalized MP4 is probed; cumulative `sum(probedDurationsMs)` becomes the global caption offset. When upstream passes per-scene `captionChunks` + `wordTimings` + preset id (the new V26.13 path), the composer rebuilds the ASS post-normalize using those probed offsets, replacing the upstream pre-built `captionsAssContent`. Eliminates the gradual drift the user observed (PixVerse audio stretching, mux frame-rounding, fps=30 cfr re-timing — all sources of small per-clip drift that compounded across scenes). (12) **V26.SEC** — comprehensive security audit, 5 findings all fixed: **CRITICAL** dead `/api/render/start` route accepted `userId` from request body with no auth → deleted (no source callers); **HIGH** `/api/render/[jobId]/status` + `/api/render/[jobId]/events` (SSE) had no ownership check → both now resolve `getOrCreateAppUser()` and enforce `job.userId === dbUser.id` before returning data; **MEDIUM** `/api/demo/start` was unauthenticated public DB write + Redis enqueue → added `getOrCreateAppUser()` gate; **MEDIUM** `safeFetch` `redirect: 'follow'` allowed SSRF redirect-chain bypass (public URL → 302 → `127.0.0.1` / `169.254.169.254` AWS metadata) → switched to `redirect: 'manual'` with explicit re-validation of every hop's hostname against `isPrivateOrLocalHost()`, capped at 5 hops. Audit also confirmed: zero `$queryRaw`/`$executeRaw`, zero `spawn/exec` with user input, zero hardcoded secrets, all admin pages behind `(admin)/layout.tsx → requireAdmin()`, all `/api/admin/*` have `requireAdminApi()`, all paid Server Actions verify ownership via `userId: dbUser.id` filters.
- V27.10.6 — script-gen default flipped Haiku 4.5 → Sonnet 4.6 (`apps/web/lib/llm/anthropic-script-client.ts`). The wrapper sends `output_config.effort` + `thinking: { type: 'disabled' }`; Haiku 4.5 returns `400 invalid_request_error: This model does not support the effort parameter`, Sonnet 4.6 (and Opus 4.7) accepts it. Reproduced via `/api/admin/diag/anthropic`. Live measurement after revert: simple ping <1s; full-shape script-gen call (system+schema+effort:low+thinking:disabled+max_tokens 8192) succeeds. Speed cost vs Haiku is real but bounded by `effort: 'low'` + `thinking: disabled` + prompt caching on the cache_control'd system block (first batch writes the cache, subsequent batches in the 5-min window read at ~10% input cost). Pricing already maps `claude-sonnet-4-6` → $3/$15 per MTok in `lib/usage/pricing.ts`; `attributeAnthropicTextCost` mirrors it. Override via `ANTHROPIC_SCRIPT_MODEL` env if the user wants Opus 4.7 (quality++ / cost 5x) or Haiku 4.5 (speed++ ONLY after also patching the wrapper to drop `output_config.effort` for Haiku — leaving the patch un-done is what bit V27.10.2). API rate-limit safety: 6 parallel calls × 8192 max_tokens stays under Sonnet tier-1 (50 RPM / 30k input TPM / 8k output TPM) on a cold batch. CLAUDE.md / STATUS.md / version line bumped here per house rule.
- V27.4 Wave 3 final — canonical /scripts → /scenes hop wired with View Transition. The form-action redirect pattern was holding back the brief's named target ("script chosen → scenes born"). Solution (Vercel/Linear pattern): action returns `{ ok, redirectTo }` instead of calling `redirect()`; client component (`continue-button.tsx`) wraps `router.push` in `document.startViewTransition` after the action resolves. Any future `revalidatePath` runs BEFORE returning so the new route renders fresh data. **All 4 wizard hops are now View Transition–wrapped**: /scripts → /scenes (V27.4, this entry, ContinueButton), /scenes → /voices (V27.3, TransitionLink), /voices → /videos (V27.3, TransitionLink), plus 2 persistent elements (WizardProgressStrip + Topbar credits-meter). The wizard is visually one continuous flow. **Lesson** (load-bearing for future me): browser-native APIs (View Transitions, Container Queries, Anchor Positioning, View-Timeline) are stable Web Platform features. They do NOT depend on React-stable wrappers. I lost a wave shrinking from `document.startViewTransition` because I conflated it with React 19's `unstable_ViewTransition` — different APIs entirely. Don't defer adoption for hypothetical React churn.
- V27.2 Wave 3 partial — only the **View Transitions API CSS infrastructure** lands. `view-transition-name: --vt-wizard-progress-strip` declared on the WizardProgressStrip wrapper. When the browser supports View Transitions (Chrome 111+, Safari 18+) AND same-origin nav happens, the strip auto-persists across step 3→4. No-op fallback in older browsers. No JS behavior change. The full `unstable_ViewTransition` React 19 wrapper for staged orchestration (selected-script-card carrying its title forward, scene-card crossfade) is **deferred** until React 19's API stabilizes — wiring it pre-stable is a known trap. **Other Wave 3 items explicitly NOT shipped per V27 brief**: drag-to-reorder scenes (no product evidence; scenes are LLM-ordered), framer-motion activation (no use case View Transitions can't cover), light mode (deliberate dark-only brand choice — not a backlog item), premium Hebrew typography upgrade (Ploni / Almoni / Fraktion — budget decision, defer until needed), i18n beyond Hebrew/English (not on roadmap). These deferrals match the brief's "Out of Scope" exactly; revisit only if product evidence supports.
- V27 Wave 2 — extends Wave 1 across non-wizard surfaces. **Chrome**: Topbar (tier-surface, primary credits chrome, success-tone admin pill, glow-primary avatar), Sidebar (dense, tier-surface, primary-tone active with glow-primary, kicker-muted section labels, status icons text-success/text-primary, motion-pulse-ai only on the actively-routed project), AdminSidebar (same pattern; removed inverted bg-foreground "loud" admin treatment in favor of tier-surface neutral chrome), DashboardAurora (radial blob colors retuned 258 92 / 290 92 / 78 82, opacity 50→40). **Pages**: Library = comfortable Krea-mode showcase (tier-elevated tiles + motion-tilt-hover + container-showcase max-width); Pricing = popular card uses tier-elevated + edge-gradient-primary, current uses tier-elevated + border-success, default uses tier-surface + motion-lift-hover; Settings = dense Vercel-mode with font-mono fields + intent="action" upgrade CTA; Admin overview = kicker-muted heading + KpiCard rewired (tier-surface default, success-soft accented healthy metric). `(admin)/layout.tsx` declares `data-density="dense"` at the layout root so every admin sub-route inherits Vercel-mode by default. Sub-pages (admin/users/queue/renders/projects/costs) sweep section kickers to `kicker-muted font-mono` (was inline `text-muted-foreground tracking-widest`). Side sweeps: `from-accent / to-accent / via-accent` gradient utilities (4 files) → `from-ai / to-ai / via-ai`; bare `glass` class instances (12 files) → `tier-elevated`. **Wave 2 deferred to Wave 3**: View Transitions API for wizard step 3→4 nav (waiting on `<unstable_ViewTransition>` to stabilize); drag-to-reorder scenes; framer-motion activation; light mode; premium Hebrew typography (Ploni / Almoni). **Reserved view-transition-name targets** documented in globals.css `--vt-*` namespace stay reserved for Wave 3.
- V27 Wave 1 — Tri-Modal Liquid visual-language redesign (tokens + wizard end-to-end). Foundation overhaul of `apps/web/app/globals.css` + `tailwind.config.ts` + `apps/web/app/layout.tsx`. Three coordinated modes share one vocabulary: **Vercel-mode** (Precision · Engineered · Restraint — chrome, admin, settings, sidebar, library), **Krea-mode** (Cinematic · Generative · Alive — wizard step 4-7, scene cards, video reveal), **Granola-mode** (Liquid · Atmospheric · Connective — modals, sheets, popovers, AI-active states). Mode is declared via `data-density="dense|default|comfortable|showcase"` on a wrapping element; CSS variants cascade through the subtree (`<DensityScope>` helper at `apps/web/components/density/density-scope.tsx`, dev-time downward-only inheritance guard). **Color contract: color and motion are state, not decoration.** `--ai` (78° lime) appears only inside `[data-ai-active="image|voice|clip|script-batch|render"]` containers (auto-applies `glow-ai` + `motion-pulse-ai` via globals.css; `render` is static — pulsing for 5+ minutes is painful). `--success` (150° green) appears only on completion (`[data-state="success"]`, 800ms static fade). Six-step elevation ladder at constant 228° 6%, only L varies. Primary desat 100→92, h1 weight 900→800 (Heebo caps at 800; previous 900 fell to system fallback). Surface tier system replaces ad-hoc glass: `tier-surface` (no blur, Vercel chrome), `tier-elevated` (8px blur, default cards), `tier-overlay` (20px, popovers/sheets, edge-pearl gradient on top edge), `tier-atmosphere` (28px, modal scrims + featured hero + RenderStatusPanel), `tier-liquid` (40px, **landing hero only**). Hard limit max 2 glass tiers per screen. Twelve named motion patterns (motion-{fade,fade-up,pop-in,slide-down,slide-side,cinematic-reveal,press,lift-hover,tilt-hover,shimmer,pulse-ai,aurora}); `motion-press` (state confirmation) is exempt from `prefers-reduced-motion`. `<Button intent="default|action|hero">` (36/44/52px) for semantic CTA hierarchy; legacy `size` preserved for back-compat. `<SectionKicker variant="loud|muted">` explicit (no context-aware magic) — `loud` tracking-[0.24em] text-primary, `muted` tracking-[0.18em] text-fg-tertiary. **Fonts**: IBM Plex Sans → Geist Sans (Vercel-mode DNA, tabular numbers built in — fixes /admin/costs alignment); JetBrains Mono → Geist Mono. Heebo + Geist Sans + Geist Mono. **Sweep complete** (Stage 5): zero `glass-strong` / `glass-liquid` / `tachles-*` keyframes / `bg-accent` / `text-accent` / `accent-foreground` / `IBM_Plex_Sans` / `JetBrains_Mono` references in source. `scripts/check-v27-legacy.sh --strict` ready for CI gating. Kept as aliases for Wave 2 sweep: `.shadow-glow`, `.shadow-glow-accent`, `.tilt-hover`, `.gradient-border`, `.card-hover`, `.bg-mesh*`. **Wave 1 scope**: tokens + 8-step wizard end-to-end; Topbar/Sidebar/Admin/Library/Dashboard chrome **NOT** restyled (Wave 2). Reserved view-transition-name targets documented in globals.css for Wave 2 (`--vt-wizard-progress-strip` / `--vt-selected-script` / `--vt-scene-card-{id}` / `--vt-final-video-poster` / `--vt-cta-generate` / `--vt-credits-meter`). Source of truth: `.design/design-language-v27/{DESIGN_BRIEF,DESIGN_TOKENS,TASKS}.md`.
- V26 — Per-scene clip-engine choice (Kling vs xAI / Grok Imagine). `apps/web/lib/animation/grok-imagine.ts` (NEW) implements `VideoGenerationProvider` against `POST /v1/videos/generations` (submit) + `GET /v1/videos/{request_id}` (poll every 5s, 10 min cap) on `https://api.x.ai/v1`. Image-to-video only — accepts public HTTPS URL or base64 data-URI; reference frames + native negative_prompt aren't documented on the video API yet, so the provider folds negatives into the main prompt as "AVOID: ...". xAI URLs are ephemeral so `generateImageToVideo` downloads bytes inline before returning. `lib/scenes/clip-impl.ts` reads `Scene.clipProvider` pre-flight: `'grok'` opt-in routes through `grokImagineProvider`, anything else stays on Kling. Lipsync scenes are pinned to Kling (PixVerse face-gate is wired only against Kling output) — a `'grok'` request on a lipsync scene logs the fallback and continues with Kling. The same column doubles as post-flight record (overwritten with `'grok'`/`'kling'` on success). Per-call cost goes through new `attributeGrokVideoCost` (per-second pricing × duration; $0.08/s 480p, $0.15/s 720p defaults via `XAI_VIDEO_PRICE_PER_SEC_{480P,720P}_USD`). `lib/providers/balance.ts` adds `fetchXaiBalance()` returning a sentinel error (xAI doesn't expose per-key billing on the video API), wired into `fetchAllProviderBalances` so `/admin/costs` renders an xAI fallback card backed by local `ApiCall` aggregates. UI: a `<ClipProviderToggle>` Kling/Grok pill row appears on each `<SceneClipCard>` above the LipSync toggle in step 5; the Grok button is disabled with a Hebrew tooltip when the scene requires lipsync. New env: `XAI_API_KEY` (Bearer), optional `XAI_VIDEO_MODEL` (default `grok-imagine-video`), `XAI_VIDEO_RESOLUTION` (default `720p`), `XAI_API_BASE_URL` (default `https://api.x.ai/v1`). Reference doc lives at `.claude/skills/xai-video-api.md`. Tachles credits unchanged — both providers roll up to the same `kling_i2v_clip` user charge (provider-cost differences fall on us).

---

## Repo layout

```
ugc-video-platform/
├── apps/
│   ├── web/                    Next.js app
│   │   ├── app/                App Router (pages + API routes)
│   │   │   ├── (admin)/admin/  costs · projects · queue · renders · users
│   │   │   ├── (auth)/         login · register
│   │   │   ├── (dashboard)/    dashboard · library · pricing · projects/[id]/* · settings
│   │   │   └── api/            REST endpoints (see API Routes below)
│   │   ├── lib/
│   │   │   ├── animation/      face-gate · kling · lipsync (PixVerse) · motion-analysis · scene-routing
│   │   │   ├── auth/           Supabase + sync-user + admin promotion
│   │   │   ├── image-briefs/   deterministic brief builder + corrective-brief
│   │   │   ├── llm/            scripts.ts (6-batch) · scene-images.ts (gpt-image-2)
│   │   │   ├── plans.ts        PLAN_CONFIGS + PER_OPERATION_CREDITS
│   │   │   ├── pricing/        provider-costs.ts (central USD + credit constants)
│   │   │   ├── product-intelligence/  dossier · visual-analysis · audience-inference
│   │   │   ├── scenes/         generate-impl · voice-impl · clip-impl · regen-prompt · mux-audio
│   │   │   ├── scraper/        cheerio + JSON-LD + OG + Shopify + microdata + quick-suggest
│   │   │   ├── storage/        index.ts (auto-select) · r2.ts (Cloudflare R2) · local.ts (dev)
│   │   │   ├── usage/          rate-limit · spend-cap · log (two-phase ApiCall) · credits · pricing
│   │   │   ├── voice/          elevenlabs.ts + voice-presets (30 voices)
│   │   │   ├── db.ts           Prisma client + per-query duration logging ([SLOW QUERY] >500ms)
│   │   │   └── timing.ts       timed() wrapper — logs [TIMING] / [SLOW] for any async op
│   │   └── public/             avatars/ (25 PNGs) · voice-samples/ (30 MP3s) · music/ (17 tracks)
│   │
│   └── worker/
│       ├── Dockerfile          Node 20 + ffmpeg, used by Railway
│       └── src/
│           ├── processors/
│           │   ├── render-processor.ts   V6+ render flow (composition-only)
│           │   └── kling-sweep.ts        hourly stuck-task sweep
│           └── providers/composition/ffmpeg.ts   3-stage low-mem pipeline (per-clip normalize → concat-demuxer → optional overlay) + R2 upload of final MP4
│
├── packages/
│   ├── shared/src/
│   │   ├── music/              music-library.ts (17 tracks) + select-music.ts
│   │   └── captions/           types · chunker · ass-builder · presets (5 V12 presets)
│   └── prompts/src/
│       ├── script-system-prompt.ts   V5 (Hebrew + Israeli realism + Product Intelligence)
│       ├── script-json-schema.ts     structured-output schema
│       ├── scene-image-prompts.ts    avatar + product ref wrapper
│       └── scene-safety.ts           23 risky→safe rewrites + modesty tokens
│
├── prisma/
│   ├── schema.prisma           9 models, 6 enums
│   └── migrations/             18 migrations (Apr 27–29 2026)
│
├── vercel.json                 framework + buildCommand + regions: ["bom1"]
├── railway.toml                Worker deployment config (Dockerfile-based)
└── apps/web/next.config.mjs    transpilePackages + experimental.staleTimes.dynamic = 0
```

---

## Pipeline (V12 — all stages real, no mocks in active path)

```
URL → scrape (cheerio + Shopify + OG + microdata)
    → Product Intelligence (dossier · visual analysis · audience inference) [gpt-5.4-mini + gpt-4o-mini]
    → Avatar selection (25-portrait local catalog)
    → Scripts ×6 in parallel [gpt-5.4-mini structured output]
        • V5 creative_strategy (17 fields) + 12-axis quality_score
        • Selective regen if overall < 8
    → Scene images [gpt-image-2 medium 1024×1792]
        • Image Brief Builder (deterministic, no LLM)
        • Single-pass — V13 PR1 removed the post-gen QA + auto-regen loop
    → Voice [ElevenLabs eleven_v3 with-timestamps]
        • charactersToWords (Hebrew/niqqud aware)
        • chunkCaptions (2–5 words, ≤2 lines, 650–2200ms)
        • ffprobe-measured duration
    → Clip [Kling Omni v3 i2v]
        • Motion analysis (gpt-4o-mini vision, cached per imageUrl)
        • Face gate (gpt-4o-mini vision) → PixVerse LipSync if mouth visible
        • Otherwise: ffmpeg mux (silent clip + voice MP3)
    → Final render [BullMQ → ffmpeg, 3-stage low-mem pipeline]
        • 3a. Per-clip normalize in series (libx264 main/3.1, aac 44.1k, fps=30 cfr) — one decoder+encoder at a time
        • 3b. concat-demuxer + `-c copy` (safe because 3a locked codec params byte-identical)
        • 3c. Optional overlay pass — captions re-encode video, music re-encodes audio, anything not needed is stream-copied
        • Music (17-track Mixkit library, mood-aware scoring)
        • Captions (ASS v4+ burn-in via libass, 5 presets)
        → /uploads/finals/<ts>.mp4
```

---

## API Routes

| Method | Path | Purpose |
|--------|------|---------|
| POST | `/api/products/extract` | Scrape + Product Intelligence |
| GET/PATCH | `/api/scenes/[id]` | Scene CRUD |
| POST | `/api/scenes/[id]/generate` | Generate scene image |
| POST | `/api/scenes/[id]/voice` | Generate scene voice |
| POST | `/api/scenes/[id]/clip` | Generate scene clip (Kling + optional PixVerse) |
| POST | `/api/scenes/[id]/regen-prompt` | Prompt-only regen |
| GET | `/api/projects/[id]/scripts/list` | List scripts |
| POST | `/api/projects/[id]/render` | Enqueue final render |
| GET | `/api/render/[jobId]/status` | Poll render progress |
| POST | `/api/render/start` | Start render (alt entry) |
| GET | `/api/voice/sample/[voiceId]` | Stream voice preview |
| GET | `/api/health` | Health check |
| POST | `/api/demo/start` | Demo mode entry |

---

## Database models (Prisma)

| Model | Key columns / notes |
|-------|---------------------|
| `User` | plan (free_trial/creator/brand/agency) · creditsBalance · spendCapUsd · banned |
| `CreditTransaction` | append-only audit log, signed amount, reason, ref, adminId |
| `ApiCall` | two-phase: status in_progress→success/failed · provider · costUsd · tokens |
| `Project` | productData JSON (wizard state + intelligence + avatar + music/caption toggles) |
| `Script` | framework (string not enum) · rawJson (V5 strategy + scenes + quality_score) |
| `Scene` | 50+ columns: image/voice/clip URLs + in-flight timestamps + face-gate + PixVerse IDs + caption chunks + image-QA artifacts |
| `RenderJob` | pending → extracting_assets → composing_video → uploading_final → completed/failed |
| `Asset` | type enum: product_image/voice_audio/avatar_video/broll_video/composition/final_video/thumbnail/background_music |

---

## Credits & pricing (lib/plans.ts + lib/pricing/provider-costs.ts)

| Plan | Credits/mo | Price |
|------|-----------|-------|
| free_trial | 30 one-time | — |
| creator | 500 | $49/mo |
| brand | 1800 | $149/mo |
| agency | 6000 | $499/mo |

Key operations (credits): script gen · scene image · voice · clip (Kling) · lipsync (PixVerse). First regen free. Spend cap enforced per-user (default in spend-cap.ts, overridable by admin).

---

## Dev commands

```bash
npm run dev:web          # Next.js dev server
npm run dev:worker       # BullMQ worker (tsx watch)
npm run typecheck        # all workspaces
npm run prisma:generate  # after schema changes
npm run prisma:migrate   # apply migrations
npm run prisma:studio    # Prisma Studio GUI
npm run test:render      # worker render tests
```

---

## Environment variables (root .env / .env.local)

Web + worker share the same .env at repo root.

**Required (always):** `DATABASE_URL` · `REDIS_URL` · `NEXT_PUBLIC_SUPABASE_URL` · `NEXT_PUBLIC_SUPABASE_ANON_KEY` · `SUPABASE_SERVICE_ROLE_KEY` · `OPENAI_API_KEY` · `ELEVENLABS_API_KEY` · `KLING_ACCESS_KEY` · `KLING_SECRET_KEY` · `PIXVERSE_API_KEY` · `PUBLIC_BASE_URL`

**Required in production (R2 storage):** `CLOUDFLARE_R2_ACCOUNT_ID` · `CLOUDFLARE_R2_ACCESS_KEY_ID` · `CLOUDFLARE_R2_SECRET_ACCESS_KEY` · `CLOUDFLARE_R2_BUCKET_NAME` · `CLOUDFLARE_R2_PUBLIC_URL` — when `CLOUDFLARE_R2_BUCKET_NAME` is set, `lib/storage/index.ts` switches from local disk to R2 automatically.

**Optional:** `ADMIN_EMAILS` (comma-separated) · `WORKER_CONCURRENCY` · `OPENAI_SCRIPT_MODEL` (default `gpt-5.4-mini`) · `OPENAI_FACE_GATE_MODEL` (default `gpt-4o-mini`)

---

## Key patterns & conventions

- **No mocks in active path.** `mock.ts` files exist in provider dirs as templates only, never instantiated.
- **In-flight timestamps** (`imageInFlightAt`, `voiceInFlightAt`, `clipInFlightAt`) prevent double-clicks from triggering duplicate provider calls. Always set before calling a provider, clear on finish.
- **Two-phase ApiCall logging** — insert row with `status: "in_progress"` at start, update to `"success"/"failed"` on finish. Never log only on success.
- **Motion cache** — `clipMotionTaskId/ImageUrl/GeneratedAt` skip Kling i2v re-run on lipsync-only retries. Invalidate when `scene.imageUrl` changes.
- **Face gate** — gpt-4o-mini vision before PixVerse; skip lipsync if no mouth. Cache result per `faceGateImageUrl`.
- **Hebrew TTS text** — use `scene.textHebrewTts` (cleaned) not `textHebrew` (raw display) when calling ElevenLabs.
- **ASS captions** — built from `captionChunksJson` (scene-relative ms) offset to global timeline in render-processor. Never fall back to proportional estimation.
- **Music** — honor `productData.backgroundMusic` toggle; use `musicProfile` from `script.rawJson` for scoring.
- **Flow toggles for captions + music** — `apps/web/app/(dashboard)/projects/[id]/layout.tsx` renders a persistent `<ProjectFlowToggles>` bar on every project subpage (overview, scripts, avatar, scenes, videos, finish). Flipping a toggle calls `setProjectFlowToggle()` from `flow-toggle-actions.ts`, which writes `productData.captions` / `productData.backgroundMusic` and `revalidatePath`s the project layout. The client also `router.refresh()`es so the videos page's `<CaptionPresetPicker>` (gated on `productData.captions === true`) appears or disappears immediately. Initial values come from the new-project wizard at `/projects/new`; the flow bar is the only way to change them after project creation.
- **Prisma** — always `await prisma.$disconnect()` in worker scripts. Use `onDelete: Cascade` for child rows. **Every query is logged** with its duration via `lib/db.ts`; queries >500ms get a `[SLOW QUERY]` tag for grep'ability.
- **Storage** — never hardcode `/public/uploads/...` paths. Always go through `getStorage()` from `lib/storage/index.ts` so dev (local FS) and prod (R2) both work.
- **Reading public assets in API routes** — `public/` is excluded from the Vercel function bundle (`next.config.mjs` `outputFileTracingExcludes`). NEVER do `fs.readFile(path.join(process.cwd(), 'public', ...))` directly. Always go through `readPublicAsset()` / `readPublicAssetAsDataUrl()` from `lib/storage/read-public-asset.ts` — it tries disk first (dev), falls back to HTTP fetch via `PUBLIC_BASE_URL` (Vercel CDN), and passes absolute http(s) URLs through. V12.1–V12.3 fixed 9 helpers that violated this; do not regress.
- **Static catalogs (avatars / music / voice samples)** — hard-coded R2 URLs in `apps/web/lib/avatars/catalog.ts`, `packages/shared/src/music/music-library.ts`, `apps/web/lib/voice/voice-presets.ts`. Run `npx tsx apps/web/scripts/upload-static-assets-to-r2.ts` after adding new assets to `apps/web/public/{avatars,music,voice-samples}/`. The R2 public URL `https://pub-eb116bdbeab8486f96ecf7c4fbc1014a.r2.dev` is intentionally hard-coded — it's a CDN endpoint, not a secret, and avoids `NEXT_PUBLIC_*` env juggling for client-component imports.
- **ffmpeg in the web app** — Vercel serverless has no `ffmpeg`/`ffprobe` on PATH. Always invoke them via `FFMPEG_BIN`/`FFPROBE_BIN` constants from `lib/scenes/mux-audio.ts` (which resolves to `ffmpeg-static` / `ffprobe-static` bundled binaries). The binaries are explicitly included in `next.config.mjs` `outputFileTracingIncludes` so Vercel bundles them. The worker has its own apt-installed ffmpeg and is unaffected.
- **Long-running Server Actions** — any page whose Server Action might exceed 60s (e.g. `scripts/generate`, multi-scene batch ops) MUST `export const maxDuration = 120` from the page.tsx, NOT the actions.ts (Next.js rejects it there). Without it, Vercel kills the function and the client hangs in pending forever.
- **Region pinning** — `vercel.json` `regions: ["bom1"]` is load-bearing. Don't change it without ALSO migrating the Supabase project to a matching region — every cross-region query costs ~250ms.
- **Performance instrumentation** — wrap any new async DB/network op with `timed('label', () => fn())` from `lib/timing.ts` so its duration shows in Vercel logs. Search logs for `[TIMING]` / `[SLOW]` to find bottlenecks.
- **Per-call cost attribution (V13.2)** — call `attribute<Provider>Cost(...)` from `lib/usage/cost-attribution.ts` to compute `costUsd` for any provider call. Prefer provider-reported usage (tokens, chars, credits); fall back to configured formulas/constants. NEVER derive per-call cost from balance deltas (fetching live balance before/after the call) — that approach is broken under concurrency, creates rate-limit pressure, and is explicitly forbidden by `FORBIDDEN_balanceDeltaAttribution()`. Provider live balances (`lib/providers/balance-snapshot.ts`, 60s cached) are observability + reconciliation only.
- **Admin API guard (V13.2)** — every `/api/admin/*` route MUST call `requireAdminApi()` from `lib/auth/admin-api.ts` first and return its 401/403 NextResponse on `!ok`. Page-level `requireAdmin()` from `sync-user.ts` redirects (only for pages); API routes need JSON 403 for non-admins.
- **Admin /admin/costs polling cadence** — `SummaryKpis` 20s · `InFlightCallsSection` 4s · `RecentCallsTable` 8s · provider balances 60s. Each section has its own client component that pauses on `document.visibilityState !== 'visible'`. API responses are server-side cached (15s for summary, 30s for operation-stats) so a tab burst doesn't fan out into duplicate aggregates.
- **TypeScript** — `npm workspaces` (no pnpm/yarn). Worker uses `Node` moduleResolution — import from package root, not subpaths.
- **Secrets** — never hardcode. All credentials via env vars only.

---

## What NOT to do

- Do not add mock providers or fake data to the active render/voice/clip path.
- Do not switch the ffmpeg compose back to single-pass `concat-filter`. The original concat-demuxer corruption was caused by **mixed input codec params** (different SAR, AAC profile, etc.). The current 3-stage pipeline solves both problems: stage 3a re-encodes every input to byte-identical libx264 main/3.1 + aac 44.1k 192k stereo + fps=30 cfr, so 3b's concat-demuxer with `-c copy` cannot hit the corruption. Reverting to a 6-input `concat-filter` reproduces the Railway OOM-kill we hit at frame ~75 (N parallel decoders + libass + amix all in RAM). Verified: 2026-04-30, ffmpeg.ts.
- Do not use proportional caption timing — always use real word timings from ElevenLabs.
- Do not skip the in-flight timestamp pattern when adding new generation actions.
- Do not add new Prisma enums for things that might evolve — use `String` columns (see `framework`, `sceneGoal`, `sceneGenerationType`).
- Do not import from package `exports` subpaths in the worker (Node moduleResolution limitation).
- Do not move Vercel functions out of `bom1` while Supabase stays in `ap-south-1` — the cross-region latency makes every page render 2-5s of pure network wait.
- Do not put `export const maxDuration` in a `'use server'` actions.ts file — Next.js rejects it. Put it in the page.tsx that renders the form calling that action.
- Do not write final MP4s / images / voice MP3s to `apps/web/public/uploads/` in production code — Vercel's serverless filesystem is read-only between requests. Always go through `lib/storage/index.ts`.
- Do not commit the `ugc-video-platform-secrets/` directory or its `.zip` — it contains live API keys (OpenAI, Kling, PixVerse, ElevenLabs, Supabase). It's git-ignored intentionally.
- Do not put a `startCommand` in `railway.toml` — it silently overrides the Dockerfile `CMD`. Cost us a deploy where the .toml had a stale path that conflicted with the Dockerfile's WORKDIR, producing duplicated `apps/worker/apps/worker/...` paths and ERR_MODULE_NOT_FOUND crashes.
- Do not pre-compile the worker's TypeScript expecting that to remove the tsx runtime requirement — the workspace packages (`@ugc-video/shared`, `@ugc-video/prompts`) declare `"main": "./src/index.ts"`, so the worker still imports `.ts` files at runtime and tsx is mandatory.
- Do not call `fs.readFile(process.cwd() + '/public/...')` from anywhere outside `lib/storage/local.ts` and `lib/storage/read-public-asset.ts`. On Vercel that path resolves to `/var/task/apps/web/public/...` which doesn't exist (public/ is excluded from the function bundle). Use `readPublicAsset()` instead.
- Do not store new static assets only on disk — always also push them to R2 via the upload script. The `public/` folder is now a dev convenience; production reads everything from R2 CDN.
- Do not compute per-call provider cost from balance deltas. `attribute<Provider>Cost` in `lib/usage/cost-attribution.ts` is the only sanctioned path. Balance-delta attribution is unsafe under concurrency (multiple in-flight calls bleed into each other), creates rate-limit pressure on provider /balance APIs, and makes tests non-deterministic. There's a deliberately-throwing `FORBIDDEN_balanceDeltaAttribution()` and a verification assertion in `test-v13-pr10.ts` to keep this honest.
- Do not call `prisma.providerBalanceSnapshot.create*` outside `lib/providers/balance-snapshot.ts`. The cache + 60s TTL + soft-fail handling lives there; ad-hoc fetches will hammer provider APIs and trigger 429s.
- Do not select `ApiCall.metadata` in list views. The recent-calls API returns it ONLY when `?expand=metadata` is on, and per-row drilldowns lazy-fetch. Bringing the JSON into a 50-row table makes the response 50-200KB and slows the dashboard.
